# mq-lite

轻量单机消息队列,使用 Go 标准库实现,零第三方运行时依赖。

> 项目处于骨架阶段(0 代码),以下为设计目标,尚未实现。

## 设计目标

- 广播 + 独立游标尾随读(每订阅者维护自己的 offset)
- 可插拔存储接口(内存实现起步,WAL 持久化后置)
- 自研二进制帧协议(纯编解码,与网络解耦)
- 内嵌内核库,或独立 TCP 服务(标准库实现)两种用法

## Roadmap

单机核心(广播) → ack / 消费组 → WAL 持久化 → raft 集群。

## License

[MIT](LICENSE)
